/**
 * ArrayLists
 * 
 * @author Sam
 */
public class ArrayListExamples {
	
	/*
	 * Idea: Like arrays, but more powerful and unweildly
	 * 
	 * 
	 * Comparisons to arrays:
	 * 		Variable size (vs fixed size)
	 * 		Holds objects (vs objects + primitives)
	 * 		No limit on how much memory it could take (vs fixed when created)
	 * 		Slower
	 * 		Much more flexible and powerful
	 * 
	 * 		Can do anything an array can:
	 * 			- GET: arr[index] --> arrList.get(index)
	 * 			- SET: arr[index] = x --> arrList.set(index, element)
	 * 			- SIZE: arr.length --> arrList.size()
	 * 		Can do extra things:
	 * 			- ADD: arrList.add(element)
	 * 			- REMOVE: arrList.remove(element), arrList.remove(index)
	 *			- CLEAR: arrList.clear() --> removes everything
	 *			- INDEXOF: arrList.indexOf(element)
	 *			- ISEMPTY: arrList.isEmpty()
	 *
	 * How to use:
	 * 		How to initialize
	 * 		Enhanced for loop
	 *
	 */
	 
	
    public static void main (String[] args) {
    	
    }
}
